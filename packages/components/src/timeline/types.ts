/**
 * Defines the timeline mode.
 *
 * - `left` to get timeline items to the left of the timeline.
 * - `right` to get timeline items to the right of the timeline.
 * - `alternate` to alternate timeline items from left to right.
 */
export type TimelineMode = "left" | "right" | "alternate";
