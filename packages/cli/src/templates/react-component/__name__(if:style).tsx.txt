import { forwardRef } from "react";

import classes from "./__name__(kebabCase).module.scss";

export interface __name__Props {}

/**
 * Component display name.
 */
const COMPONENT_NAME = "__name__";

/**
 * Component default props.
 */
const DEFAULT_PROPS: Partial<__name__Props> = {};

const __name__ = forwardRef<HTMLDivElement, __name__Props>((props, ref) => (
  <div ref={ref} className={classes.__name__(kebabCase)} {...props}>
    Hello __name__
  </div>
));
__name__.displayName = COMPONENT_NAME;
__name__.defaultProps = DEFAULT_PROPS;

export default __name__;
